pipeline {
    agent none 
    stages {
        stage('Example Build') {
            options{ 
                        timeout(time: 4, unit: 'SECONDS')
                    }
            steps {
				sctipt {
					echo 'Hello, Skull'
					echo '${PROVIDER_MANIFEST}'
				}
            }
        }
        stage('Example Test') {
            steps {
                echo 'Hello, JDK'
            }
        }
    }
}
