pipeline {
    agent none 
    stages {
        stage('Example Build') {
            options{ 
                        timeout(time: 4, unit: 'SECONDS')
                    }
            steps {
                echo 'Hello, Maven'
            }
        }
        stage('Example Test') {
            steps {
                echo 'Hello, JDK'
            }
        }
    }
}